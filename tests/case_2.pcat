PROGRAM IS
    VAR A, B : REAL := 0.0; C : REAL := 0.0; 
BEGIN 
    WRITE ("ENTER TWO REALS:");
    READ (A, B);
    C := 8.0;
    WRITE ("A=", A, ", B=", B, ", C=", C);
    C := A - (-B) + C;
    C := 4 + 7 / 9 + (3 + 5 * 2);
    WRITE ( -C /(A + 1));  
END;
